---
# vCenter credentials sourced from environment variables (do not hardcode here)
vmware:
  host: '{{ lookup("env", "VMWARE_HOST") }}'
  username: '{{ lookup("env", "VMWARE_USER") }}'
  password: '{{ lookup("env", "VMWARE_PASSWORD") }}'
