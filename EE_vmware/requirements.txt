# Python packages required at runtime inside the EE.
#
# ansible-builder introspects collection python_requires but does not always
# install every transitive dep into the final image layer. List critical
# packages explicitly to guarantee they are present.

# VMware SDK — required by community.vmware (pyVim / pyVmomi submodules)
pyvmomi>=9.1.0.0

# WinRM connectivity for ansible.windows (NTLM transport used in playbooks)
pywinrm>=0.4.3
requests-ntlm>=1.3.0
