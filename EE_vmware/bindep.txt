# System-level binary dependencies resolved by bindep
# Format: <package> [platform:condition]

# Required to compile Python C extensions (pyvmomi, cryptography, etc.)
gcc [platform:rpm]
python3-devel [platform:rpm]

# SSL/TLS libraries for HTTPS connections to vCenter and AAP
openssl-devel [platform:rpm]
libffi-devel [platform:rpm]

# Kerberos libraries — needed if using Kerberos WinRM transport
krb5-devel [platform:rpm]
krb5-workstation [platform:rpm]
